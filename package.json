{
  "$schema": "https://json.schemastore.org/package",
  "name": "goal",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "biome:check": "bunx biome check .",
    "biome:fix": "bunx biome check . --write",
    "test:apps": "bun test ./apps --jsx-import-source hono/jsx",
    "typecheck": "bunx tsc -b --noEmit"
  },
  "private": true,
  "devDependencies": {
    "@biomejs/biome": "^2.4.13",
    "@tsconfig/bun": "^1.0.10",
    "@types/bun": "^1.3.12",
    "typescript": "^6.0.3"
  },
  "version": "1.0.0"
}
