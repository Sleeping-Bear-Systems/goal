# goal

To install dependencies:

```sh
bun install
```

To format the codebase:

```sh
bun run format
```

To run:

```sh
bun run dev
```

open <http://localhost:3000>

Workspace packages live under:

- `apps/*`
- `packages/*`
